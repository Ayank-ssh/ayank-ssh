AYANK PROFILE README — CONTRIBUTION HISTORY V6

Fixed:
- GitHub GraphQL schema error: totalContributions belongs to contributionCalendar.
- Queries one calendar year at a time.
- Generates the full available history from account creation through the current year.
- Keeps the red/crimson palette and hoverable contribution cells.

Push:
git add .
git commit -m "Fix contribution history GraphQL query"
git push

Then:
GitHub -> Actions -> Sync Contribution Graph -> Run workflow -> main
