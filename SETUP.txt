AYANK PROFILE README — V8

Changes:
- Removed the awkward "Hover any cell..." paragraph.
- Replaced the ugly image/badge composition with one clean CTA button.
- Redesigned the contribution graph to be larger and easier to read.
- One year per row, GitHub-style month + weekday labels.
- Larger rounded cells and per-year contribution totals.
- Same black/crimson/red palette.
- Hover cells for exact date + contribution count.
- Graph automatically syncs through GitHub Actions.

Push:
  git add .
  git commit -m "Polish contribution graph layout"
  git push

Then:
  Actions -> Sync Contribution Graph -> Run workflow -> main
