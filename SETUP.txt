AYANK PROFILE — CONTRIBUTION GRAPH V7

The previous workflow had stale code from an older version of the script.
V7 replaces the script completely.

It:
- gets the account creation year
- queries GitHub one calendar year at a time
- preserves all available contribution history
- generates the red/crimson contribution SVG
- adds hover tooltips for every contribution cell

Push:
  git add .
  git commit -m "Fix contribution graph sync"
  git push

Then:
  Actions -> Sync Contribution Graph -> Run workflow -> main
